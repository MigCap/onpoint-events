import React from 'react';
import { Container } from 'semantic-ui-react';

const NavBarChildren = ({ children }) => (
  <Container style={{ marginTop: '0' }}>{children}</Container>
);
export default NavBarChildren;
